#!/bin/sh
command -v git-lfs >/dev/null 2>&1 || { printf >&2 "\n%s\n%s\n%s\n%s\n%s\n\n" "==================================" "⚠  Git LFS not found — images/videos are text pointers only." "   Install:  https://git-lfs.com/" "   Then run: git lfs install && git lfs pull" "=================================="; exit 0; }
git lfs post-commit "$@"
